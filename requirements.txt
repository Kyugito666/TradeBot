# Python >= 3.10 required (dataclass slots=True)

# Exchange & networking
ccxt>=4.3.0,<5.0.0          # Unified exchange API; async_support subpackage
websockets>=12.0,<13.0      # Raw WebSocket client
aiohttp>=3.9.0,<4.0.0       # Async HTTP for REST fetchers

# Database
aiosqlite>=0.19.0,<1.0.0    # Async SQLite for StateDB

# Data processing
pandas>=2.0.0,<3.0.0        # OHLCV dataframes
numpy>=1.24.0,<3.0.0        # Numerical ops (MockOHLCVFetcher, ATR)

# Env & config
python-dotenv>=1.0.0,<2.0.0 # .env loader